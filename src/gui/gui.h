#pragma once

namespace Overboss::GUI {

    // Configures Dear ImGui with Pip-Boy dark phosphor green palette and metrics
    void ApplyPipBoyTheme();

    // Renders the mod menu overlay and HUD watermark
    void Render();

} // namespace Overboss::GUI
